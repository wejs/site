/**
 * Widget home-features-list main file
 *
 * See https://github.com/wejs/we-core/blob/master/lib/class/Widget.js for all Widget prototype functions
 */

module.exports = function homeFeaturesListWidget(projectPath, Widget) {
  var widget = new Widget('home-features-list', __dirname);
  return widget;
};