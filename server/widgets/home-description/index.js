/**
 * Widget home-description main file
 *
 * See https://github.com/wejs/we-core/blob/master/lib/class/Widget.js for all Widget prototype functions
 */

module.exports = function homeDescriptionWidget(projectPath, Widget) {
  var widget = new Widget('home-description', __dirname);

  return widget;
};